# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# --- Virtual environments ---
.venv/
.venv-*/
venv/
env/

# --- Secrets / credentials ---
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*-key.json
service-account*.json
gcp-credentials*.json
application_default_credentials.json

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
*~
.idea/
*.iml

# --- Node / frontend (future) ---
node_modules/
dist/
build/
.next/
*.log
npm-debug.log*
yarn-error.log

# --- Infrastructure as Code (Feature 009 — Terraform) ---
.terraform/
# NOTE: .terraform.lock.hcl is intentionally NOT ignored — Terraform's own guidance is to
# commit it so provider version selection is reproducible across machines.
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- Docker ---
*.dockerignore

# --- Jupyter ---
.jupyter/
profile_default/
ipython_config.py

# --- Lock files (not pinned at project level — regenerate from requirements) ---
# uv.lock is intentionally NOT ignored so the environment is reproducible.

# --- Spec Kit transient state ---
.specify/feature.json

# --- Raw third-party data (OCWD dataset, ~39M) ---
# Not version-controlled. Source: Harvard Dataverse "orange_county_ro".
# See docs/02-data-pipeline.md for provenance and download instructions.
dataverse_files/
